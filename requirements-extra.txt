# mysql_pipeline/ + deep_learning/ 에서만 추가로 필요한 패키지.
# 기존 requirements.txt는 건드리지 않고, 아래를 추가 설치하세요:
#   pip install -r requirements-extra.txt

# --- MySQL 연동 (mysql_pipeline/) ---
PyMySQL==1.1.1          # SQLAlchemy용 MySQL 드라이버
python-dotenv==1.0.1    # .env 에서 MYSQL_* 접속 정보 로드
pyarrow==24.0.0          # model_table_final 로컬 스냅샷을 parquet로 저장/로드 (mysql_pipeline/03, deep_learning/*)

# --- 딥러닝 (deep_learning/) ---
tensorflow==2.17.0       # Tabular MLP (Keras). PyTorch를 선호하면 torch로 대체 가능

