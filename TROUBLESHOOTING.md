# 트러블슈팅 정리

프로젝트를 진행하며 실제로 겪었던 이슈와 해결 과정을 데이터/모델링 단계와 백엔드/프론트엔드 개발 단계로 나눠 정리했습니다. 모두 실제 노트북·코드·대화 기록에 근거한 내용만 담았습니다.

---

## A. 데이터/모델링 단계

### A-1. 시점 누출(Data Leakage) 방지
**문제**: 이탈 예측처럼 미래 라벨을 맞추는 문제에서는, 피처를 만들 때 실수로 라벨이 결정되는 시점 이후의 정보가 섞여 들어가면 실제로는 재현 불가능한 성능이 나온다.
**해결**: 관측 기준일을 **2017-01-31**로 못박고, `members`/`transactions`/`user_logs` 모든 피처엔지니어링 단계에서 이 날짜 이후 데이터를 명시적으로 제외했다 (예: transactions는 컷오프 이후 거래 908,062건 제외, user_logs는 컷오프 이후 로그 16,079,619건 제외). 통계 기반 결측치 대체(중앙값 등)도 train split에서만 계산해 valid/test에 동일 값을 적용하는 방식으로, split 간 정보 누출도 차단했다.

### A-2. MySQL에 원본 raw CSV 직접 적재 시도 실패 → DuckDB로 전환
**문제**: 원래는 `members`/`transactions`/`user_logs`/`train` 원본 CSV를 MySQL에 그대로 적재해두고 SQL로 분석하려 했으나, 특히 `user_logs.csv`(약 30GB, 3억 9천만 행)를 MySQL에 적재하는 시도가 현실적으로 되지 않았다(용량·처리 시간 문제로 완료하지 못함 — 정확한 에러 로그는 남아있지 않아 필요하면 팀 내 공유 부탁드립니다).
**해결**: raw 데이터를 MySQL에 적재하는 방식 자체를 포기하고, 대신 DuckDB로 로컬 CSV를 SQL처럼 직접 질의하는 방식(`analytics/01_sql_feature_mart`)으로 전환했다. 이 방식으로 기존 pandas 청크 처리 결과와 100% 일치를 확인했고 속도도 훨씬 빨랐다(143.5초 vs pandas 약 1,820초). 이후 설계 방향도 명확해져서, MySQL 서빙 DB(`kkbox_serving`)에는 **원본 raw 데이터를 절대 올리지 않고 "모델 예측 결과 + 참조 테이블 + 계정"만 적재**하는 구조로 확정했다 (`DB_ERD_가이드.md`에 이 원칙이 명시되어 있음).

### A-3. 30GB / 3.9억 행 user_logs 처리
**문제**: `user_logs.csv`는 약 30GB(3억 9천만 행)라 한 번에 메모리로 읽을 수 없었다.
**해결**: 1,000만 행 단위 청크로 순회하며 통계를 누적하는 방식으로 EDA와 피처엔지니어링을 수행했고, 중간 결과는 pickle 체크포인트로 저장해 재계산 비용을 줄였다. 이후 `analytics/01_sql_feature_mart`에서 DuckDB로 동일 집계를 SQL로 재검증한 결과 100% 일치했고, 처리 속도는 DuckDB가 143.5초로 기존 pandas 청크 방식(약 1,820초) 대비 훨씬 빨라, 향후에는 SQL 엔진 활용을 우선 고려하기로 했다.

### A-4. 서빙 파이프라인에서의 메모리 부족(ArrowMemoryError)
**문제**: `build_scoring_table.py`에서 전체 인구(약 99만 명) 스코어링용 테이블(`model_table_enhanced_v2.csv`, 590MB)을 한 번에 읽으면 메모리 부족 에러가 발생했다.
**해결**: 10만 행 단위 청크로 나눠 예측을 수행하도록 수정했다 (코드 주석에 원인이 명시되어 있음: "590MB 테이블을 한 번에 다 읽으면 메모리 부족(ArrowMemoryError)이 나서 나눠서 처리").

### A-5. 로그 최근성 파생 피처의 극단값
**문제**: `trend_secs_recent15_vs_prior15`(최근 15일 대비 이전 15일 재생초 변화율) 피처는 분모(이전 15일 평균)가 0에 가까운 유저에게서 변화율이 최대 16만 배 수준까지 폭주하는 경우가 있었다.
**해결**: 트리 기반 모델(LightGBM 등)은 이런 극단값에 비교적 강건하다고 판단해 그대로 사용했지만, 노트북에 "거리 기반/선형 모델에 재사용하거나 다른 용도로 쓸 때는 winsorize/클리핑을 고려할 것"이라고 명시적으로 캐비아트를 남겼다.

### A-6. 모델 간 비교에 LogLoss/threshold를 그대로 쓸 수 없는 문제
**문제**: XGBoost/CatBoost/RandomForest/LogisticRegression은 각각 `scale_pos_weight`/`auto_class_weights`/`class_weight="balanced"` 등으로 클래스 재조정을 해서 학습했지만 LightGBM은 재조정 없이 학습했다. 그 결과 valid F1-최적 threshold가 모델마다 크게 달랐다 (LightGBM ≈0.27~0.35 vs 나머지 ≈0.7~0.9). LogLoss나 threshold를 그대로 모델 간 비교에 쓰면 잘못된 결론이 나올 수 있었다.
**해결**: threshold·스케일에 덜 민감한 AUC/PR-AUC/F1을 모델 비교의 기준으로 삼고, "모델 선택은 AUC 하나가 아니라 PR-AUC, F1, 학습시간, 해석 가능성을 함께 고려한다"는 원칙을 노트북에 명시했다.

### A-7. 딥러닝(MLP) 클래스 가중치 실험
**문제**: 이탈:잔존 비율(1:14.64)을 그대로 `pos_weight`에 반영했더니 오히려 valid PR-AUC가 기대만큼 좋지 않았다.
**해결**: `pos_weight=14.64`(그대로) → `3.83`(제곱근 완화) → `1.0`(미적용) 순으로 실험했고, 실제로는 가중치를 아예 적용하지 않은 모델(`pos_weight=1.0`)의 valid PR-AUC가 가장 높아 이를 최종으로 채택했다. 다만 이 MLP도 LightGBM 대비 PR-AUC·Recall·F1이 낮아 최종 프로덕션 모델로는 채택하지 않았다.

### A-8. Enhanced v1 → v2 채택 판단 기준
**문제**: 파생 피처를 추가한다고 항상 성능이 좋아지는 건 아니라서, "추가할 가치가 있다"는 판단 기준이 필요했다.
**해결**: AUC, PR-AUC, LogLoss 세 지표 모두가 개선되는지를 채택 기준으로 삼았다. v1(파생변수 16개 추가, PR-AUC +0.009)과 v2(로그 최근성 피처 1개 추가, PR-AUC 추가 +0.0014)는 모두 이 기준을 통과해 최종적으로 v2(57개 피처)가 `model_role: "final churn prediction model"`로 채택됐다.

### A-9. 대시보드용 산출물과 실서빙 모델 버전 불일치 (미해결, 확인 필요)
**문제**: 프로젝트 산출물을 정리하는 과정에서 `streamlit_app/data/ltv_summary.json`의 `risk_threshold` 값(0.35498)이, 실제 서빙 백엔드(`build_scoring_table.py`)가 쓰는 enhanced v2 모델의 threshold(0.2708)와 다르다는 것을 발견했다. 0.35498은 구(舊) 베이스라인 LightGBM(파생피처 추가 이전 모델)의 threshold와 일치한다.
**현재 상태**: 이 문서를 정리하며 새로 발견한 사항으로, 아직 해결하지 못했다. 대시보드가 모델 v1→v2 전환 이전 시점의 산출물을 그대로 쓰고 있을 가능성이 있어, **발표 전 팀 내에서 어느 수치가 최신인지 확인이 필요하다.**

### A-10. `models/model_comparison.csv` 파일 값이 노트북 자체 출력과 불일치 (신규 발견, 미해결)
**문제**: 이 문서를 준비하며 `modeling/03_model_comparison.ipynb`가 실제로 무엇을 출력했는지 노트북 원문(코드 셀 11번, `model_comparison.csv`를 직접 저장하는 셀)을 확인한 결과, 노트북이 그 자리에서 직접 print한 test 성능(예: LightGBM AUC 0.9011 / PR-AUC 0.5753 / F1 0.5508)과, 실제로 저장소에 있는 `models/model_comparison.csv` 파일 안의 값(예: LightGBM test AUC 0.9915 / PR-AUC 0.9066 / F1 0.8118)이 서로 다르다는 것을 발견했다. 노트북 12번 셀에서 계산한 "train-test AUC gap"(LightGBM 기준 0.0194, 즉 train AUC ≈ 0.9205)도 현재 CSV 파일의 train AUC(0.998)와 앞뒤가 맞지 않는다.
**현재 상태**: 아직 원인을 특정하지 못했다(다른 실행 결과로 파일이 덮어써졌을 가능성 등으로 추정). **해결 전까지는 노트북이 직접 출력한 수치(0.90 AUC대)를 신뢰할 수 있는 값으로 보고 이 문서/발표자료에 사용했으며, `model_comparison.csv` 파일의 0.99대 수치는 사용하지 않았다.** 발표 전 팀 내에서 `03_model_comparison.ipynb`를 다시 실행해 파일을 갱신하는 것을 권장한다.

---

## B. 백엔드 트러블슈팅

### B-1. 서빙 DB의 전체 재적재 구조와 "로그인 자격증명 추가" 아이디어
**문제**: 발표 시연을 더 쉽게 하려고 "고객ID + 고정 비밀번호(1234)"를 고객 로그인에 추가하는 방안을 검토했다.
**확인**: `customer_churn_scores` 테이블은 `load_to_mysql.py` 실행 시마다 `TRUNCATE` 후 CSV에서 전량 재적재되는 구조였다 (반면 `staff_accounts`/`customer_actions`는 이 재적재 대상에서 제외되어 있음, 코드 주석으로 이미 명시돼 있던 설계). 이 테이블에 로그인 자격증명 컬럼을 얹으면 파이프라인을 재실행할 때마다 값이 초기화되거나, 별도 테이블 분리 등 구조가 복잡해지는 문제가 있었다.
**결론**: 원본 데이터 자체에 로그인 자격증명이 없었다는 점, 그리고 복잡도 대비 얻는 이득이 적다는 점에서 **이 기능은 도입하지 않기로 결정**했다. (현재의 "체험 로그인"— msno 존재 여부만 확인 —구조를 그대로 유지)

### B-2. 죽은 엔드포인트 발견
**문제**: 프론트/백엔드 코드를 정리하며 관리자 콘솔의 실제 fetch 호출 URL과 백엔드 라우터를 전수 대조한 결과, `POST /admin/customers/{msno}/actions` 엔드포인트가 프론트 어디에서도 호출되지 않는다는 것을 발견했다.
**확인**: 개별 고객 대상 액션 발송은 이미 `POST /admin/campaigns`(`selection_mode="manual"`)로 통합되어 있었다.
**해결**: 엔드포인트 자체는 Swagger 테스트용으로 남겨두되, 독스트링에 "[관리자 화면 미사용]"이라고 명시해 향후 혼동을 방지했다.

### B-3. 중복 스크립트 발견
**문제**: `backend/scoring/add_lifecycle_status.py`가 `lifecycle_status` 컬럼을 계산하는데, `build_scoring_table.py`의 5b 단계가 이미 CSV 생성 시점에 동일 로직으로 이 컬럼을 채우고 있어 사실상 중복이었다.
**해결**: 스크립트를 삭제하지는 않되(과거 DB에 컬럼만 급하게 채워야 하는 예외 상황 대비), 독스트링에 "[레거시/선택 실행]"이라 명시하고 언제 필요 없는지를 정리했다.

---

## C. 프론트엔드 UI 트러블슈팅

### C-1. CSS 커스텀 프로퍼티 상속 버그 (다크/라이트 텍스트 대비 문제의 근본 원인)
**문제**: "다크모드일 땐 잘 보이고 라이트모드일 땐 안 보이고, 반대인 경우도 있다"는 리포트를 받고 조사한 결과, `body { color: var(--text); }` 규칙이 사실상 무효라는 걸 발견했다. `--text` 변수는 `.viz-root`/`.cx-theme-light`에 정의되는데, 이 두 클래스는 `body`의 **자손**이지 조상이 아니라서 CSS 커스텀 프로퍼티가 위로 거슬러 올라가 적용될 수 없었다. 그 결과 `body`의 실제 계산된 `color`는 브라우저 기본값(검정)으로 떨어지고, 이 값이 자기 `color`를 명시하지 않은 하위 요소들에 그대로 상속되고 있었다.
**시행착오**: 처음에는 `.viz-root` 선택자를 `:root, .viz-root`로 바꿔서 `body`가 변수 스코프를 보게 하려 했으나, Playwright로 라이트 모드를 직접 검증해보니 이번엔 `body`의 색이 (더 바깥의 `:root` 스코프에서) 고정값으로 정해져 버려 `.cx-theme-light`가 적용된 하위 트리의 오버라이드를 보지 못하는 반대 문제가 생겼다. 그래서 이 변경은 되돌렸다.
**최종 해결**: `body`를 고치는 대신, 문제가 되는 개별 리프 요소(예: 플레이어바 곡 제목 `.cx-playerbar-title`)에 `color: var(--text)`를 직접 명시하는 방식으로 고쳤다. 다크/라이트 양쪽 모두 헤드리스 브라우저로 계산된 색상값을 직접 검증한 뒤 반영했다.

### C-2. 다크 전용 카드의 대비 문제
**문제**: `.cx-billing-plan-card`, `.cx-winback-card`처럼 배경이 테마와 무관하게 항상 고정된(다크/골드 그라데이션 등) 카드들은, 안의 텍스트가 `var(--text)` 계열(테마에 따라 바뀌는 변수)을 쓰면 라이트 모드에서 배경은 그대로인데 글자색만 밝아져 안 보이는 문제가 있었다.
**해결**: 이런 "항상 고정 배경" 카드들은 테마 변수 대신 고정 색상(예: `#f3f7f6`, `rgba(243,247,246,0.62)`)을 명시하도록 통일했다.

### C-3. 로그인 화면의 테마 분리
**문제**: 앱 기본 테마를 라이트로 바꾼 뒤에도, 로그인 화면만은 항상 라이트 톤으로 고정해달라는 요청이 있었다(이후 앱 내부의 다크/라이트 토글과 무관하게).
**해결**: `LoginScreen` 컴포넌트의 반환 JSX를 `<div class="cx-theme-light">`로 감싸, 상위 앱의 테마 상태와 무관하게 항상 라이트 스타일이 적용되도록 분리했다.

### C-4. 관리자 msno 검색과 기존 필터의 충돌
**문제**: 관리자 콘솔에 고객 ID(msno) 검색 기능을 추가할 때, 기존의 위험군/세그먼트/생애주기 필터가 동시에 켜져 있으면 "검색한 고객이 필터 조건에 안 맞아서 안 보이는" 것처럼 발표 중 혼동을 줄 수 있는 상황이 우려됐다.
**해결**: msno 검색이 입력되면 다른 필터는 서버 요청에서 아예 제외하고 UI에서도 흐리게 비활성화(`.ac-filters-disabled`)해, 검색 중에는 검색 결과만 보이도록 명확히 분리했다. 검색어는 350ms 디바운스를 적용해 매 타이핑마다 요청이 나가지 않도록 했다.

### C-5. 알림함 UX 개편
**문제**: 기존 "나의 리마인드" 탭 + 개별 알림 리스트 구조가, 실제 SNS(페이스북/인스타그램)형 알림함에 익숙한 사용자에게는 직관적이지 않다는 피드백이 있었다.
**해결**: 벨 아이콘 클릭 시 우측에서 슬라이드인되는 알림 패널(`NotificationPanel`)로 전면 개편하고, 항목 클릭 시 관련 혜택 화면으로 바로 이동하도록 했다. 이 과정에서 "나의 리마인드" 탭은 완전히 제거했다.

### C-6. 재구독(win-back) 할인가 계산
**문제**: 장기이탈 고객에게 재구독을 유도하는 카드에서, 실제 쿠폰 할인율을 요금제 가격에 반영해서 보여달라는 요청이 있었는데, 기존 쿠폰 데이터(`COUPON_RULES`)에는 할인율이 별도 필드로 없고 안내 문구(`title`)에만 텍스트로 섞여 있었다.
**해결**: 문자열 파싱 대신, 실제로 정률 할인이 존재하는 세그먼트에만 `discountPct` 필드를 새로 추가해(다른 세그먼트는 건드리지 않음) 요금제 가격에 프로그래밍적으로 곱해서 계산하도록 했다. 데이터를 새로 지어내지 않고 기존 비즈니스 룰에 필드 하나만 보강하는 방식을 택했다.

### C-7. 세션 작업물이 실제 파일에서 사라진 것 발견 (원인 추정, 팀 확인 필요)
**문제**: README/트러블슈팅 문서를 준비하며 실제 파일을 다시 불러와 확인한 결과, 이전에 작업했던 다크/라이트 대비 수정, 플레이어 셔플·반복·시크, 알림 패널 개편, win-back 카드, 관리자 msno 검색 등이 `kkbox_customer.html`/`kkbox_admin.html`/`admin_router.py`에 더 이상 남아있지 않다는 것을 발견했다.
**추정 원인**: 해당 변경들이 git에 커밋되지 않은 상태였는데, 같은 날 다른 팀원이 "만료기간" 관련 수정을 하며 git pull/checkout 등을 실행하는 과정에서 로컬의 미커밋 변경이 되돌아간 것으로 추정된다(정확한 원인은 미확인).
**해결/교훈**: 작업이 끝나는 대로 바로바로 커밋하는 습관이 필요하다는 것을 다시 확인했다. 현재는 확인된 손실 항목 중 일부(홈 화면 히어로 타이틀 줄바꿈 들여쓰기, 상단 여백)만 재작업을 시작했고, 나머지 항목의 재작업 여부는 팀 논의가 필요하다.

---

## 요약

데이터/모델링 단계에서는 "시점 누출 방지"와 "대용량 데이터 처리"가 반복적으로 등장하는 핵심 이슈였고, 백엔드/프론트엔드 단계에서는 "실제로 있는 데이터/로직만으로 UX를 개선하는 것"과 "테마·필터처럼 서로 다른 상태가 충돌하지 않게 만드는 것"이 핵심이었습니다. A-9(대시보드-서빙 모델 버전 불일치), A-10(model_comparison.csv 불일치), C-7(세션 작업물 소실)은 아직 해결되지 않은 항목이니 발표 전 확인이 필요합니다.
