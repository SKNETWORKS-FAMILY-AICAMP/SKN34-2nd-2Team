# 트러블슈팅 정리

프로젝트를 진행하며 실제로 겪었던 이슈와 해결 과정을 데이터/모델링 단계와 백엔드/프론트엔드 개발 단계로 나눠 정리했습니다. 모두 실제 노트북·코드·대화 기록에 근거한 내용만 담았습니다.

---

## A. 데이터/모델링 단계

### A-1. 시점 누출(Data Leakage) 방지
**문제**: 초기 전처리에서는 `transactions.csv`와 `user_logs.csv`에 들어 있는 **2017-02-28까지의 기록을 모두 사용**해 피처를 만들었다. 그런데 `train.csv`의 예측 대상은 2월 만료 고객이 만료 후 30일 안에 재구독했는지 여부이므로, 2월의 결제·이용 기록은 단순한 과거 행동이 아니라 일부 고객의 재구독 또는 이탈 결과가 이미 진행된 기간의 정보였다. 실제 분석에서도 `days_to_expire` 하나의 directional AUC가 약 **0.905**였고, 관측일 기준 만료 후 30일이 지난 고객의 이탈률이 약 **99.2%**로 나타났다. 초기 MLP 역시 Validation ROC-AUC 약 **0.988**, PR-AUC 약 **0.886**으로 지나치게 높은 성능을 보였다. 이는 모델이 이탈의 선행 신호만 학습한 것이 아니라, 2월 말 시점에 이미 드러난 만료·재결제 상태를 이용해 정답에 가까운 정보를 읽고 있을 가능성이 크다는 신호였다. 이 상태의 성능은 실제로 2월이 시작되기 전에 고객 이탈을 예측하는 운영 환경에서 재현할 수 없었다.

**해결**: 관측 기준일을 **2017-01-31**로 못박고, `members`/`transactions`/`user_logs` 모든 피처엔지니어링 단계에서 이 날짜 이후 데이터를 명시적으로 제외했다 (예: transactions는 컷오프 이후 거래 908,062건 제외, user_logs는 컷오프 이후 로그 16,079,619건 제외). 통계 기반 결측치 대체(중앙값 등)도 train split에서만 계산해 valid/test에 동일 값을 적용하는 방식으로, split 간 정보 누출도 차단했다.

### A-2. 원본 데이터 DB 적재 실패와 서빙 DB 역할 재설계
**문제**: 처음에는 `members`, `transactions`, `user_logs`, `train` 원본 CSV를 MySQL에 모두 적재한 뒤 SQL로 전처리와 분석을 수행하려 했다. 그러나 약 30GB, 3억 9천만 행 규모의 `user_logs.csv`까지 관계형 DB에 적재하는 과정은 저장 공간과 처리 시간이 지나치게 커 프로젝트 일정 안에 완료하기 어려웠다. 분석용 원본 데이터와 웹 서비스에 필요한 결과 데이터를 하나의 MySQL DB에서 모두 관리하려던 구조 자체가 과도했다.

**해결**: MySQL의 역할을 원본 분석 저장소가 아닌 **서비스 제공용 서빙 DB**로 한정했다. 대용량 원본 CSV는 로컬 분석 파이프라인에서 처리하고, MySQL `kkbox_serving`에는 고객별 모델 예측 결과, 세그먼트, 대시보드 참조 테이블, 캠페인 기록과 계정 정보만 적재하도록 구조를 변경했다. 이 결정으로 웹 서비스가 30GB 원본 로그에 직접 의존하지 않게 했으며, 분석 파이프라인과 서비스 운영 계층의 책임도 분리했다.

### A-3. 30GB 로그의 청크 처리와 SQL 교차 검증
**문제**: DB 적재 여부와 별개로 `user_logs.csv`는 약 30GB, 3억 9천만 행이어서 pandas로 파일 전체를 한 번에 메모리에 올릴 수 없었다. 중간에 처리가 중단되면 처음부터 다시 계산해야 하는 시간 비용도 컸고, 청크별 누적 집계가 정확한지 검증할 방법도 필요했다.

**해결**: pandas에서 1,000만 행 단위로 파일을 읽어 고객별 이용 통계를 누적하고, 중간 결과를 pickle 체크포인트로 저장해 재실행 비용을 줄였다. 이후 `analytics/01_sql_feature_mart`에서 DuckDB가 원본 CSV를 직접 읽어 동일한 집계를 수행하도록 만들었다. pandas 결과와 DuckDB SQL 결과의 행 수 및 주요 집계값이 100% 일치하는 것을 확인했으며, 처리 시간도 DuckDB 약 143.5초, pandas 청크 방식 약 1,820초로 측정돼 향후 대용량 집계에는 SQL 엔진을 우선 고려할 근거를 확보했다.

### A-4. 서빙 파이프라인에서의 메모리 부족(ArrowMemoryError)
**문제**: `build_scoring_table.py`에서 전체 인구(약 99만 명) 스코어링용 테이블(`model_table_enhanced_v2.csv`, 590MB)을 한 번에 읽으면 메모리 부족 에러가 발생했다.

**해결**: 10만 행 단위 청크로 나눠 예측을 수행하도록 수정했다 (코드 주석에 원인이 명시되어 있음: "590MB 테이블을 한 번에 다 읽으면 메모리 부족(ArrowMemoryError)이 나서 나눠서 처리").

### A-5. 모델 간 비교에 LogLoss/threshold를 그대로 쓸 수 없는 문제
**문제**: XGBoost/CatBoost/RandomForest/LogisticRegression은 각각 `scale_pos_weight`/`auto_class_weights`/`class_weight="balanced"` 등으로 클래스 재조정을 해서 학습했지만 LightGBM은 재조정 없이 학습했다. 그 결과 valid F1-최적 threshold가 모델마다 크게 달랐다 (LightGBM ≈0.27~0.35 vs 나머지 ≈0.7~0.9). LogLoss나 threshold를 그대로 모델 간 비교에 쓰면 잘못된 결론이 나올 수 있었다.

**해결**: threshold·스케일에 덜 민감한 AUC/PR-AUC/F1을 모델 비교의 기준으로 삼고, "모델 선택은 AUC 하나가 아니라 PR-AUC, F1, 학습시간, 해석 가능성을 함께 고려한다"는 원칙을 노트북에 명시했다.

### A-6. 딥러닝(MLP) 클래스 가중치 실험
**문제**: 이탈:잔존 비율(1:14.64)을 그대로 `pos_weight`에 반영했더니 오히려 valid PR-AUC가 기대만큼 좋지 않았다.

**해결**: `pos_weight=14.64`(그대로) → `3.83`(제곱근 완화) → `1.0`(미적용) 순으로 실험했고, 실제로는 가중치를 아예 적용하지 않은 모델(`pos_weight=1.0`)의 valid PR-AUC가 가장 높아 이를 최종으로 채택했다. 다만 이 MLP도 LightGBM 대비 PR-AUC·Recall·F1이 낮아 최종 프로덕션 모델로는 채택하지 않았다.

### A-7. Enhanced v1 → v2 채택 판단 기준
**문제**: 파생 피처를 추가한다고 항상 성능이 좋아지는 건 아니라서, "추가할 가치가 있다"는 판단 기준이 필요했다.

**해결**: AUC, PR-AUC, LogLoss를 함께 확인해 파생 피처의 채택 여부를 판단했다. v1에서 파생 피처를 추가해 성능을 높였고, v2에서는 로그 최근성 피처를 추가해 validation PR-AUC가 약 0.00075 추가 개선됐다. 최종 재실행 결과를 반영한 v2(57개 피처)가 `model_role: "final churn prediction model"`로 채택됐다.

---

## B. 백엔드 트러블슈팅

### B-1. 서빙 DB의 전체 재적재 구조와 "로그인 자격증명 추가" 아이디어
**문제**: 발표 시연을 더 쉽게 하려고 "고객ID + 고정 비밀번호(1234)"를 고객 로그인에 추가하는 방안을 검토했다.

**확인**: `customer_churn_scores` 테이블은 `load_to_mysql.py` 실행 시마다 `TRUNCATE` 후 CSV에서 전량 재적재되는 구조였다 (반면 `staff_accounts`/`customer_actions`는 이 재적재 대상에서 제외되어 있음, 코드 주석으로 이미 명시돼 있던 설계). 이 테이블에 로그인 자격증명 컬럼을 얹으면 파이프라인을 재실행할 때마다 값이 초기화되거나, 별도 테이블 분리 등 구조가 복잡해지는 문제가 있었다.

**결론**: 원본 데이터 자체에 로그인 자격증명이 없었다는 점, 그리고 복잡도 대비 얻는 이득이 적다는 점에서 **이 기능은 도입하지 않기로 결정**했다. (현재의 "체험 로그인"— msno 존재 여부만 확인 —구조를 그대로 유지)

### B-2. 죽은 엔드포인트 발견
**문제**: 프론트/백엔드 코드를 정리하며 관리자 콘솔의 실제 fetch 호출 URL과 백엔드 라우터를 전수 대조한 결과, `POST /admin/customers/{msno}/actions` 엔드포인트가 프론트 어디에서도 호출되지 않는다는 것을 발견했다.

**확인**: 개별 고객 대상 액션 발송은 이미 `POST /admin/campaigns`(`selection_mode="manual"`)로 통합되어 있었다.

**해결**: 엔드포인트 자체는 Swagger 테스트용으로 남겨두되, 독스트링에 "[관리자 화면 미사용]"이라고 명시해 향후 혼동을 방지했다.

### B-3. 중복 스크립트 발견
**문제**: `backend/scoring/add_lifecycle_status.py`가 `lifecycle_status` 컬럼을 계산하는데, `build_scoring_table.py`의 5b 단계가 이미 CSV 생성 시점에 동일 로직으로 이 컬럼을 채우고 있어 사실상 중복이었다.

**해결**: 스크립트를 삭제하지는 않되(과거 DB에 컬럼만 급하게 채워야 하는 예외 상황 대비), 독스트링에 "[레거시/선택 실행]"이라 명시하고 언제 필요 없는지를 정리했다.

### B-4. `D--312` 표시를 통해 발견한 만료 고객 세그먼트 혼합
**문제**: 관리자 고객 목록에서 만료일까지 남은 일수를 단순히 `D-${days_to_expire}`로 출력하면서 `days_to_expire=-312`인 고객이 `D--312`로 표시됐다. 이 값은 “만료까지 -312일”이 아니라 **관측 기준일인 2017-01-31보다 312일 전에 이미 만료된 고객**이라는 뜻이었다. 그런데 기존 위험도·가치 세그먼트만 사용하면 이러한 장기 만료 고객도 현재 구독 중인 고위험 고객과 같은 Retention 대상에 섞일 수 있었다. 장기 만료 고객에게 일반 갱신 리마인드를 보내는 것은 고객 상태와 캠페인 목적이 맞지 않는 문제였다.

**해결**: 해당 고객을 이상치로 일괄 삭제하지 않고 `days_to_expire`를 기준으로 구독 생명주기를 별도 분류했다. `days_to_expire > 0`은 `구독활성`, `-30 <= days_to_expire <= 0`은 `갱신유예기간`, `days_to_expire < -30`은 `장기만료`, 결측값은 `상태확인필요`로 구분했다. 이후 캠페인도 구독활성 고객은 `Retention`, 갱신유예기간 고객은 `긴급 갱신`, 장기만료 고객은 `Win-back`으로 분리했다. 프론트에서는 음수를 다시 `D-` 형식에 붙이지 않고 `만료 후 N일` 또는 `장기 만료`로 표시하도록 통일했으며, 캠페인 생성 시 목적과 생명주기가 일치하는 고객만 선택되도록 백엔드에서 검증한다.

---

## 요약

데이터·모델링 단계에서는 시점 누출 방지, 대용량 로그 처리, 불균형 데이터에 맞는 모델 평가가 핵심 이슈였습니다. 백엔드 단계에서는 분석용 원본 데이터와 서빙 데이터의 역할을 분리하고, 고객의 구독 생명주기와 캠페인 목적이 일치하도록 운영 로직을 구성하는 것이 핵심이었습니다.
